name: Lint Dokerfile

on:
  push:
  pull_request:

jobs:
  lint-doker:
    name: Lint DokerFiles
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: hadolint/hadolint-action@v3.1.0
        with: 
          dockerfile: app/Dokerfile

